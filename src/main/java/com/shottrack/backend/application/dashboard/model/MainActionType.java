package com.shottrack.backend.application.dashboard.model;

/**
 * UC42: ação principal do dashboard — continuar a visita em andamento ou,
 * se não houver, o convite genérico pra iniciar uma.
 */
public enum MainActionType {
    CONTINUE_VISIT,
    START_VISIT
}
