package com.shottrack.backend.application.dashboard.model;

/**
 * UC42: pendências de configuração inicial, calculadas do estado real do
 * atleta (sem tabela de progresso própria).
 */
public enum OnboardingStep {
    CREATE_PROFILE,
    CONFIGURE_MODALITIES,
    REGISTER_WEAPON
}
